spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/conteociclico?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=America/Bogota&zeroDateTimeBehavior=convertToNull&rewriteBatchedStatements=true&cachePrepStmts=true&prepStmtCacheSize=250&prepStmtCacheSqlLimit=2048&useServerPrepStmts=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.time_zone=America/Bogota
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
# Hikari Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# Security
api.key=${API_KEY:pharmaser_secure_api_key_2026}
jwt.secret=${JWT_SECRET:pharmaser_secret_key_2026_default_long_string_for_security}

# Permitir cargas pesadas (hasta 50MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
server.tomcat.max-http-form-post-size=50MB

# Hibernate Batching
spring.jpa.properties.hibernate.jdbc.batch_size=500
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Producción: Seguridad de Errores y Excepciones
server.error.include-exception=false
server.error.include-stacktrace=never
server.error.include-message=always

# Automatización de Carga de Inventario
# Ruta donde el bot de Medicar deposita los archivos
app.auto-import.input-path=${RUTA_DESCARGA:C:/Botmedicar}
# Ruta donde se mueven los archivos procesados (si no se eliminan)
app.auto-import.processed-path=${RUTA_PROCESADOS:C:/Botmedicar/procesados}
# Habilitar/Deshabilitar el proceso automático
app.auto-import.enabled=${APP_AUTO_IMPORT_ENABLED:true}
# Borrar archivos después de cargar exitosamente
app.auto-import.delete-after-process=${APP_AUTO_IMPORT_DELETE_AFTER_SUCCESS:true}

# Configuración del SFTP
app.sftp.host=${SFTP_HOST:ftpharmaser.pharmaser.com.co}
app.sftp.username=${SFTP_USER:cporto}
app.sftp.password=${SFTP_PASSWORD:Ph@rm4s3r.}
app.sftp.port=${SFTP_PORT:22}
app.sftp.remote-path=${SFTP_REMOTE_PATH:/medicar/ciclicoinventario}

# Configuración del Bot
app.bot.python-path=${PYTHON_PATH}
app.bot.script-path=${BOT_SCRIPT_PATH}
